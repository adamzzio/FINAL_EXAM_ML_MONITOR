name: Update Model

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Run script
      run: python script.py
      env:
        TOKEN_SECRET_NAME: ${{ secrets.ACCESS_TOKEN }}
